package com.company;import java.util.*;public class CompanyUsers_08 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        String input = scanner.nextLine();        Map<String, List<String>> company = new TreeMap<>();        while (!input.equals("End")) {            String nameCompany = input.split(" -> ")[0];            String idEmployee = input.split(" -> ")[1];            if (!company.containsKey(nameCompany)) {                company.put(nameCompany, new ArrayList<>());                company.get(nameCompany).add(idEmployee);            } else {                //boolean containValue = false;                if (!company.get(nameCompany).contains(idEmployee) && company.containsKey(nameCompany)) {                    company.get(nameCompany).add(idEmployee);                    //containValue = true;                }                //if (!containValue){                // company.get(nameCompany).add(idEmployee);                // }            }            input = scanner.nextLine();        }        company.forEach((key, value) -> {            System.out.println(key);            value.stream()                    .sorted(String::compareTo)                    .forEach(v -> System.out.printf("-- %s%n", v));        });    }}