package com.company;import java.util.ArrayList;import java.util.Arrays;import java.util.List;import java.util.Scanner;import java.util.stream.Collectors;public class HouseParty_03 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int countCommand = Integer.parseInt(scanner.nextLine());        List<String> listOgGuest = new ArrayList<>();        for (int i = 0; i < countCommand; i++) {            String[] command = scanner.nextLine().split("\\s+");            // name} is going!"            //	"{name} is not going!"            switch (command[2]) {                case "going!":                    if (listOgGuest.contains(command[0])) {                        System.out.printf("%s is already in the list!", command[0]);                        System.out.println();                    } else {                        listOgGuest.add(command[0]);                    }                    break;                case "not":                    if (listOgGuest.contains(command[0])) {                        listOgGuest.remove(command[0]);                    } else {                        System.out.printf("%s is not in the list!", command[0]);                        System.out.println();                    }                    break;            }        }        for (String guest : listOgGuest) {            System.out.println(guest);        }    }}