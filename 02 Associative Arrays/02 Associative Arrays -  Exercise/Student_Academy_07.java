package com.company;import java.util.*;public class Student_Academy_07 {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        int n = Integer.parseInt(scanner.nextLine());        Map<String, List<Double>> studentGrade = new TreeMap<>();        Map<String, Double> averageGrade = new TreeMap<>();        for (int i = 0; i < n; i++) {            String name = scanner.nextLine();            double grade = Double.parseDouble(scanner.nextLine());            if (!studentGrade.containsKey(name)) {                studentGrade.put(name, new ArrayList<>());                studentGrade.get(name).add(grade);            } else {                studentGrade.get(name).add(grade);            }        }        for (Map.Entry<String, List<Double>> entry : studentGrade.entrySet()) {            List<Double> average = entry.getValue();            double mehmed = 0;            for (int i = 0; i < entry.getValue().size(); i++) {                mehmed += average.get(i);            }            mehmed = mehmed / entry.getValue().size();            if (mehmed >= 4.50) {                averageGrade.put(entry.getKey(), mehmed);            }        }        //studentGrade.entrySet().stream().sorted(Map.Entry.<String, Double>comparingByValue().reversed()).forEach(e -> System.out.printf("%s -> %.2f%n", e.getKey(), e.getValue()));        averageGrade.entrySet()                .stream()                .sorted((e1, e2) -> Double.compare(e2.getValue(), e1.getValue()))                .forEach(e -> System.out.printf("%s -> %.2f%n", e.getKey(), e.getValue()));    }}